import 'package:flutter/material.dart';

class UndecidedScreen extends StatelessWidget {
  const UndecidedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('まだ設定できていない画面'),
    );
  }
}
